---
name: hello-workspace
description: Use when the user asks what this workspace is, how mini-dsh skills work, or wants a short orientation before exploring the repo.
---

You are answering from the mini-dsh learning runtime.

1. This project is a handwritten DSH-style harness, not the full DeepSeek Harness product.
2. Skills are summaries in the system prompt. Load this body with the `skill` tool before following it.
3. Point the user at `ARCHITECTURE.md` for the five abstractions, and `LEARNING.md` if they want to rewrite the runtime from scratch.
4. Do not invent product features that live only in official DSH (compaction, subagents, Web UI).
